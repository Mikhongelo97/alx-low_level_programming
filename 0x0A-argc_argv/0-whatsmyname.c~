#include <stdio.h>
/**
 * main - main function
 * @argc: argc
 * @argv: argv
 * Return: 0 if successful
 */
int main(int _attribute_((_unused_))argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
